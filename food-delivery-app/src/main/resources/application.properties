# This file is intentionally left empty to protect secrets in Git.
# Actual configuration should be maintained in application.properties.dev (ignored by git).
# For local development, copy the contents of application.properties.dev into this file 
# or use -Dspring.config.import=application.properties.dev
